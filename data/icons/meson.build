icons_dir = join_paths(datadir, 'icons', 'hicolor')

install_data(
    sources: [
        '@0@.svg'.format(base_application_id),
    ],
    rename: [
        'scalable/apps/@0@.svg'.format(base_application_id),
    ],
    install_dir: icons_dir
)
