[tool.bumpversion]
current_version = "0.8"
commit = true
tag = true
parse = "(?P<major>\\d+)\\.(?P<minor>\\d+)(.(?P<patch>\\d+))?"
serialize = [
    "{major}.{minor}.{patch}",
    "{major}.{minor}"
]

[[tool.bumpversion.files]]
filename = "VERSION"
